/**
 * 4. Дана строка 'I am a hero!'. Выведите в консоль количество символов в этой строке.
 */

const STR = "I am a hero!";
const STR_LENGTH = STR.length;

console.log(STR_LENGTH);
document.write(STR_LENGTH);
